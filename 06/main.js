const numOne = Number (prompt("numero um"));
const numTwo = Number (prompt("numero dois"));
const numTree = Number (prompt("numero Tres"));

const media = (numOne + numTwo + numTree) / 3;
console.log(media)